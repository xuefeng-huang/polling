version: "3"

services:    
  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: "password"
    ports:
      - "3306:3306"
    volumes:
      - ./start.sh:/docker-entrypoint-initdb.d/start.sh
